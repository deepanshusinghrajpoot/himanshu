/*


Static Keyword
--------------

lll(1). A static variable exists for the entire lifetime of the program.
lll(2). They are declared inside the class but defined outside.
lll(3). Only one copy is shared among all objects of the class.

. Static members belong to the class, not to individual objects.
. Can be accessed using object name or class name.




Static keyword is use by two way

(1). Static keyword use with variables
(2). Static Keyword use with objects


*/